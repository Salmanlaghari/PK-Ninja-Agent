fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sse-starlette>=2.1
httpx>=0.27
aiosqlite>=0.20

# System monitoring (v0.8.0 execution monitor). Optional — the monitor
# degrades gracefully (reports "unavailable") if psutil is not installed.
psutil>=5.9

# Testing (not required at runtime)
pytest>=8.0
pytest-asyncio>=0.23

# Optional AI providers — installed only if you enable them.
# google-generativeai>=0.7   # for the Gemini provider adapter
